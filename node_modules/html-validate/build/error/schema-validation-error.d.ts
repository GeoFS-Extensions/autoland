import betterAjvErrors from "@sidvind/better-ajv-errors";
import Ajv from "ajv";
import { UserError } from "./user-error";
export declare class SchemaValidationError extends UserError {
    filename: string | null;
    private obj;
    private schema;
    private errors;
    constructor(filename: string | null, message: string, obj: any, // eslint-disable-line @typescript-eslint/explicit-module-boundary-types
    schema: any, // eslint-disable-line @typescript-eslint/explicit-module-boundary-types
    errors: Ajv.ErrorObject[]);
    prettyError(): void | betterAjvErrors.IOutputError[];
}
